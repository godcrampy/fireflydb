version: '3'
services:
  fireflydb-benchmark-bitcask:
    image: fireflydb-benchmark-bitcask
    env_file:
      - ./.env
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: '1G'
